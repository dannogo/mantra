/*
-- включать картинки соответствующие мантрам, и картинку по умолчанию для новых мантр
-- ползунок для изменения максимальной высоты консоли
-- сделать проверку на наличие текста и названия данных при добавлении мантры в список
-- постоянно прокручивать console вниз, чтобы было видно текущую печатающуюся мантру
-- По двойному щелчку ставить и снимать флаг, разрешающий прокрутку консоли вниз
*/

// получить значение currentMantra, проверить, есть ли такое свойство в mantraList 
// сделать проверку допустимости вводимых данных в поле ограничений. Только целые положительные числа, иначе красная подсветка
// не забыть сделать проверку на наличие значения в поле ограничения и на наличие значения в поле currentMantra
	// Если данных не хватает, подсветить соотвтетствующие поля красным
			// Снять подсветку при событии onInput
/*
-- создать цикл, который определяет количество повторений
	-- если установлено количество повторений, сделать обычный for
	-- если установлено количество минут, использовать while
		-- вычислить который час должен быть через указаное количество минут, и при каждой итерации проверять не настало ли это время, или не прошло ли оно

	-- полученную мантру положить в цикл:
		-- создать новый элемент для мантры
		-- получить по очереди все символы мантры, и добавить их в текст элемента
			nodeValue += мантра[i]
		-- в каждой итерации должен стоять timeOut, подтягивающий время в милисекундах из глобальной переменной. 

// количество либо проверять как количество дочерних DOM элементов 
// либо инкремент при каждой итерации основного цикла

// input[type="range"] повесить событие. При изменении перезаписывать переменную содержащую количество милисекунд для таймаута во внутреннем цикле

кнопка СТАРТ 
	-- если цикл не запущен, запускает его, иначе снимает с паузы (если в качестве паузы работает большой тайм аут, удалить его)
	-- записывает в переменные название и текст мантры, режим, количество минут или раз для возможного ПОВТОР-а
кнопка СТОП
	-- установить флаг
	-- поставить break в цикле в обоих циклах (break второго уровня), посчитать или взять из счетчика количество полных повторений мантры
	-- в предварительно созданный глобальный массив или объект добавить название мантры и количество повторений. Если эта мантра включается не в первый раз, добавить колличество повторений к уже имеющемуся значению
	-- если это мантра без названия, записать ее в массив/объект, вместо ключа поставить unknown1(unknown2, ...). При выводе этой информации на экран, показывать только первые 14 символов и ... (три точки). Такую запись выделять бледным цветом. Если текст какой-нибудь из следующих мантр будет совпадать с анонимной мантрой, записать новую мантру, добавить к количеству повторений количество из анонимной мантры, и удалить эту анонимную мантру

кнопка ПАУЗА
	-- возможно поставить очень большой таймаут
	-- можно сделать рекурсивную функцию, запускающую таймаут. В таком случае при снятии с паузы нужно будет удалить таймаут и сделать return, чтобы выйти из функции
	-- можно сделать и интервал, запускающий timeout
кнопка ПОВТОР
	-- останавливает оба цикла
	-- отправляет данные в статистику
	-- запускает ту же мантру с тем же ограничением по колличеству или по времени. Если стоит ограничение по времени, вычисляет новое время, на которое ориентироваться


	
-- черновик псевдокода begin

	echo[0] - timeout(function continueLoop(){
		echo[1];
		if(!конец мантры){ // в таком случае нужно будет помечать конец мантры
			timeout(continueLoop(), 1000);
		}else{
			newLoop(){
				if(условие продолжения){
					echo[0] - timeout(continueLoop(), 1000)}
				}else{
					отправляю название и количество на статистику
				}
		}

	}, 1000)
-- черновик псевдокода end

-- чистовик псевдокода begin
после нажатия кнопки:

var cntMantra = 0;
newLoop();

function continueLoop(){
	i++;
	echo(мантра[i]);
	if(i< мантра.length){
		setTimeout(continueLoop(), задержка);
	}else{
		newLoop();
	}
}

function newLoop(){
	if(условие продолжения){
		var i = 0;
		echo(мантра[i]);
		setTimeout(continueLoop(), задержка);
	}else{
		cntMantra++;
		отправляю название и количество на статистику;
	}
}

-- чистовик псевдокода end

*/